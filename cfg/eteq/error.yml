template: typename T
requires: TenncorAPI
name: ErrorAPI
members:
  - name: super
    type: TenncorAPI<T>*
init:
  args:
    - name: super
      type: TenncorAPI<T>*
  initlist:
    super: super
funcs:
  - name: sqr_diff
    args:
    - type: const eteq::ETensor&
      name: expect
    - type: const eteq::ETensor&
      name: got
    out:
      type: eteq::ETensor
      val: return super->square(super->sub(expect,got));
