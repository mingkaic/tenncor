language: minimal
os: linux
dist: xenial
services:
  - docker
jobs:
  include:
    - stage: build test docker image
      script:
        - git checkout ${TRAVIS_BRANCH}
        - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
        - make IMAGE_REPO=${DOCKER_USERNAME} IMAGE_TAG=travis-${TRAVIS_COMMIT} build_test_image
        - docker images
        - make IMAGE_REPO=${DOCKER_USERNAME} IMAGE_TAG=travis-${TRAVIS_COMMIT} push_test_image
    - stage: test cpp
      script:
        - make test_consul_up
        - docker run --network=host -e CODECOV_TOKEN=${CODECOV_TOKEN}
          -e REMOTE_CACHE=${REMOTE_CACHE} --rm ${DOCKER_USERNAME}/tenncor-test:travis-${TRAVIS_COMMIT} all cpp
        - make test_consul_down
    - stage: test python
      script:
        - make test_consul_up
        - docker run --network=host -e CODECOV_TOKEN=${CODECOV_TOKEN}
          -e REMOTE_CACHE=${REMOTE_CACHE} --rm ${DOCKER_USERNAME}/tenncor-test:travis-${TRAVIS_COMMIT} all python
        - make test_consul_down
    - stage: build docker image with C++ and python library
      script:
        - git checkout ${TRAVIS_BRANCH}
        - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
        - make IMAGE_REPO=${DOCKER_USERNAME} IMAGE_TAG=${TRAVIS_COMMIT} REMOTE_CACHE=${REMOTE_CACHE} build_image
        - docker images
        - make IMAGE_REPO=${DOCKER_USERNAME} IMAGE_TAG=${TRAVIS_COMMIT} push_image
notifications:
  email: false
branches:
  except:
  - legacy
  # safelist
  only:
  - master
  - /experimental(-.*)?/
  - stable
